[project]
name = "space-invaders"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pygame==2.6.0",
]


[tool.uv.build-backend]
module-root = ""
namespace = true